# 维基百科 Kindle 词典 v1

把维基百科做成 Kindle 能点选查词的词典。从 dump 到成品全程自动化，见 [README](README.md)。

本 release 附两个文件：

| 文件 | 词条数 | 体积 | 内容来源 |
| --- | --- | --- | --- |
| `wikipedia-zh.mobi` | **1,284,543 条** | 323 MB | 中文维基百科，2026-09 快照 |
| `wikipedia-en.mobi` | **781,488 条** | 244 MB | 英文维基百科，2026-09 快照 |

> **v1 修订**：英文文件已更新。初版有一个致命缺陷——词典的**根索引**超过了 MOBI 的 16 位
> IDXT 偏移上限（实测 141,138 > 65,535），**设备读不到索引表，整本查不到词**。
> 原因是英文的键数（词头 + 异名）达到 468 万，把根索引推到 196 KB。
> 现已把键长限制到 20 字符，键数降到 293 万、根索引偏移 61,613（安全）。
> 详见 [docs/06 的排查记录](docs/06-verification.md)。**早期下载的英文文件请重新下载。**

## 装到 Kindle 上

1. USB 连接 Kindle，把 `.mobi` 拷进 `documents/dictionaries/`
2. 在 Kindle 上：**设置 → 语言和字典 → 字典**，选对应语言，再选中这本词典
3. 打开任意书，选中一个词即可查

装好后是**系统级词典**——读书时选词弹释义，和内置词典一样用。若没生效，重启一次 Kindle。

## 查得到什么

Kindle 按**读者选中的字符串精确匹配**词头，所以覆盖了这些写法：

| 选中 | 命中 |
| --- | --- |
| `信义区` / `信義區` | 简繁互通，都能命中 |
| `廣東` / `广东` | 同上 |
| `廣東省員警`（重定向） | 维基重定向异名 |
| `USA` / `UK` | 英文缩写重定向 |
| `信義區` | 命中 `信義區 (臺北市)`（消歧义标题已补基名别名） |

实测设计依据：中文并入了 130 万条重定向 + 90.6 万条简繁字形别名；
英文并入 397 万条异名。**别名不进正文、只进索引**，所以不占容量。

## 生成的词典是什么样

- 每个词条的释义 = 该维基条目的**首段**，已清洗 wiki 标记（模板、脚注、图片说明、表格等）
- 中文条目释义截断上限 200 字，英文 200 字符
- 全量收录，**单本 MOBI**，无需分卷

## 已知限制

- **查词长度**：Kindle 的查词上限按「词」算（约 2–3 个词内可用，超过约 4 个词只剩翻译）。
  词条标题很长的（如书名全名、`List of ...`）实际用不上，本词典里这类占很小比例。
- **释义是自动抽取**的，未经人工校订，可能残留少量处理不干净的内容。**不要作为权威依据。**
- 中文条目数占中文维基非重定向条目的 **82.1%**；被过滤的主要是首段过短的小作品（约 17.5 万条）。
- 英文为控制容量，按**跨语言链接数 ≥ 9** 筛选，且**键长不超过 20 字符**（从 690 万条取 78 万条）。
  冷门物种、赛事、小地名等显著性低的条目不在其中；超长标题也排除了——
  反正 Kindle 单次查词约 4 个词封顶，那些标题本来也查不到。

## 一个必须知道的工程约束

MOBI 的索引里 `idxt_offset` 是 **16 位**。词典的根索引要为每个叶子索引存一条记录，
而叶子数 ≈ 总键数 ÷ 每个叶子约 1400 个键。**键太多，根索引就超过 64 KB、偏移量溢出，
设备直接读不到索引表——整本词典查不到任何词**（而 kindling 自己的检查工具能正常处理，所以只有真机会暴露）。

本 release 的两个文件都已核对：中文最大偏移 62,273、英文 61,613，都在 65,535 以内，
但**余量只剩约 5%**。若自行用更新的 dump 重建，请务必核对这一项。

## 数据来源与协议

内容来自维基百科，采用
[CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/deed.zh) 分发。
各条目作者见维基百科对应条目的历史页面。词典文件内含署名页。
再分发时请保留署名页并以相同协议共享。详见 [LICENSE-DATA.md](LICENSE-DATA.md)。

本仓库**代码**采用 [MIT 许可](LICENSE)。
